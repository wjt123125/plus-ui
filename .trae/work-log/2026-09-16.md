# 2026-09-16 工作摘要

## 完成
- **drag-on-edge-insert A 范式落地并提交**（3472c50 / ebb47d1）：findEdgeAt 点到线段 <30px 取最近边 → insertOnEdge（seq/jump/branch/merge 四种 kind）；useAutoLayout 后 setNodes 替换数组同步 Pinia/DOM；project 函数跳过稀疏数组 undefined；CmpBezierEdge dragOverMe 视觉反馈
- **议题一/二规格定稿**：handoff 写入 `.trae/handoff/edge-reconnect-and-crossing.md`
- **议题二根治档尝试后否决**：自研树递归布局（useTreeLayout.ts）三轮没改对，第二轮改崩线断连，用户拍板放弃，dagre 留任
- **议题二补丁档落地并经用户浏览器验证通过**：dagre 分层照旧，跑完对每个 gateway 直接扇出分支层按 outlet 顺序（branchIndex）重排 x，嵌套子树 BFS 整体平移（~80 行后处理，覆盖含嵌套网关）；删除 weight 5/3/1 hack 与 merge constraint:false；elkjs 评估（900KB/异步 API/port 范式错位）后拍板不换
- **拖拽三路径严格分离**：拖到 placeholder 本体（12px 容差）才填槽；拖到任意 edge（含连空槽的边）一律 insertOnEdge 线上插入、空槽保留；拖空白追加根末尾。picker 面板标题恢复「线上插入」，面板打开时禁用 placeholder 高亮
- **code-sweep 死代码清扫**：删 cmp-tree.ts（117 行），11 个引用文件改从 useElTreeModel 直引 CmpNodeData；注释术语统一（方案 B/网关范式/容器范式等黑话改为现行表述）

## 未完成 / 阻塞
- 议题一①-④（移除右键删边菜单 / 选中边才显示 updater 的消歧 CSS / reconnect pending 状态机 / saveAsEl·undo pending 拦截 + Esc）：规格定稿，代码未动

## 验证
- 议题二补丁档：用户浏览器验证通过
- 本次按用户要求未跑 oxlint / vue-tsc

## 下次第一步
- 暂无，等待确定下一阶段；启动议题一时从 ① 移除右键删边菜单项开始
